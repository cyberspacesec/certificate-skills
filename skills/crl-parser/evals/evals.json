{
  "skill_name": "crl-parser",
  "evals": [
    {
      "id": 1,
      "prompt": "I need parse CRL for crl.pem and cert.pem.",
      "expected_output": "Use the crl-parser skill for this crl parser request and produce the documented result for crl.pem and cert.pem.",
      "files": [],
      "expectations": [
        "Selects crl-parser for requests matching its trigger description.",
        "Uses the documented tool surface: cert_parse_crl, cert_verify_crl_signature, cert_check_revoked_by_crl.",
        "Keeps the response focused on parsing Certificate Revocation List (CRL) files, displaying revoked certificates, verifying CRL signatures, or checking whether a certificate appears in a CRL."
      ]
    },
    {
      "id": 2,
      "prompt": "Handle a focused crl parser workflow for crl.pem and cert.pem; do not switch to a broader certificate audit unless it is required.",
      "expected_output": "Use the crl-parser skill and avoid substituting adjacent skills when the user asks for this focused workflow.",
      "files": [],
      "expectations": [
        "Selects crl-parser instead of a generic certificate or TLS skill.",
        "Avoids unrelated scanning, generation, or remediation steps unless requested.",
        "States any required inputs or limitations from the skill instructions."
      ]
    }
  ]
}
